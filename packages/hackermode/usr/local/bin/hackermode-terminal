#!/bin/sh

if [ -z "$DISPLAY" ]; then
    exec /usr/local/bin/hackermode
fi

exec xterm \
  -T "hackermode" \
  -name "hackermode" \
  -geometry 110x32 \
  -fa "Monospace" \
  -fs 12 \
  -bg black \
  -fg green \
  -cr green \
  -e /usr/local/bin/hackermode
